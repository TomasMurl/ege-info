# ## Задание 1. Повторение по счётчику
# Дано целое число `n`.
#
# Выведи строку `"Hello"` ровно `n` раз, каждую на новой строке.
#
# Ограничение: используй цикл с `range`.

n = 100

for i in range(n): # [0, 1, 2, ..., 6]
    print("Hello")
